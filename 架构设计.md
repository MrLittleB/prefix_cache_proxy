# prefix-hash-router · 架构设计（更优方案）

> 反思"策略/反代两块"的简单切分，提出更清晰、可扩展的三层架构。
> 核心主张：**Router 是核心，策略是插拔 Policy 集合，转发是 Dispatcher**。

---

## 一、为什么"策略/反代"两块还不够好

当前切为 policy/ + proxy/ 两块，但有三个问题：

1. **命名偏实现细节**：`policy`/`proxy` 是"怎么实现"，不是"承担什么职责"。
2. **策略块揉得太重**：`pick_rank()` 在一个函数里用 if/elif 硬编码了 prefix_hash /
   consistent_hash / first_rank / round_robin 多种模式 + 会话 key 优先 + 过载保护，
   扩展策略要改这个大函数。
3. **耦合了输入**：`pick_rank(headers, body, config, load, seq)` 参数多，Router 依赖
   "HTTP header + JSON body"这种具体请求形态，不便于复用/测试/换框架。

---

## 二、更优架构：三层 + 两个抽象

```
                       ┌─────────────────────────────────────┐
   HTTP 请求 ────────▶ │  Server / Ingress（接入层）           │
                       │  - 收请求、读 body、构造 RequestContext│
                       └──────────────────┬──────────────────┘
                                          │  RequestContext（不依赖 HTTP 框架）
                                          ▼
                       ┌─────────────────────────────────────┐
                       │  Router（核心路由决策，纯逻辑）         │
                       │  - 组合多个 RoutingPolicy             │
                       │  - policy(request_ctx) -> Backend    │
                       └──────────────────┬──────────────────┘
                                          │ Backend（目标抽象）
                                          ▼
                       ┌─────────────────────────────────────┐
                       │  Dispatcher / Forward（转发层）        │
                       │  - 注入 rank 头、透传 header/body/SSE  │
                       └──────────────────┬──────────────────┘
                                          ▼
                                   vLLM 多 DP 集群
```

### 三层职责
| 层 | 职责 | 关注点 |
|---|---|---|
| **Ingress（接入）** | 收 HTTP、读 body、建 RequestContext | 与 HTTP 框架适配 |
| **Router（路由）** | 纯逻辑：context → Backend | 可替换、可测、与 HTTP 无关 |
| **Dispatcher（转发）** | 把 Backend 变成实际转发 | 透传/注入/SSE/健康检查 |

### 两个核心抽象
1. **`RequestContext`**：`{ headers, body, raw_body, parsed_body, session_key, ... }`
   —— Router 与 HTTP 解耦，换 FastAPI/Flask/标准库都不影响 Router。
2. **`RoutingPolicy` 接口**：`route(ctx) -> Backend`
   —— **策略 = 可插拔 Policy 类的集合**（而非 if/elif）。每个 Policy 实现同一接口：
   - `PrefixHashPolicy`：`blake2b(稳定共享前缀) % DP`
   - `SessionAffinityPolicy`：命中显式 session key，粘性最强
   - `ConsistentHashPolicy`：哈希环，DP 扩缩容最小重映射
   - `RoundRobinPolicy`：兜底
   - `OverloadGuard`：装饰器式过载保护（可选组合）

---

## 三、RequestContext 与 Backend

```python
@dataclass
class RequestContext:
    headers: dict
    raw_body: bytes
    parsed_body: dict | None      # JSON 解析后的 Chat body
    session_key: str | None       # 预先提取，缓存

@dataclass
class Backend:
    dp_rank: int                  # 目标 DP（0..N-1）
    inject_rank: bool = True      # 是否注入 X-data-parallel-rank
    # 未来可扩展: upstream_url, health, timeout 等
```

- `RequestContext` 解耦 Router 与 HTTP。
- `Backend` 解耦 Router 与"转发如何做"；从"返回 rank int"升级为"返回目标后端"，
  为未来加多上游/健康检查/非 DP 场景留空间。

---

## 四、RoutingPolicy 接口（核心可替换点）

```python
class RoutingPolicy(Protocol):
    def route(self, ctx: RequestContext) -> Backend: ...

# 组合策略（Chain/Ensemble）:
class ChainedPolicy:
    def __init__(self, policies: list[RoutingPolicy]): ...
    def route(self, ctx) -> Backend:
        # 依次尝试：session → prefix_hash → round_robin(兜底) → overload guard
        for p in self.policies:
            b = p.route(ctx)
            if b is not None:     # None 表示"本策略不适用，交给下一个"
                return b
        raise NoBackend
```

设计要点：
- **策略可插拔**：加新策略 = 新增一个实现 RoutingPolicy 的类，不改 Router。
- **可组合**：`ChainedPolicy` 按优先级串联（session → prefix → round-robin）。
- **过载保护**：可作为包装 Policy 或 Backend 的装饰，保持正交。

---

## 五、相比"两块"方案的优势

| 维度 | 两块方案 | 三层 + 抽象方案 |
|---|---|---|
| 关注点分离 | policy/proxy 是实现细节 | Ingress/Router/Dispatcher 是职责 |
| 可替换策略 | if/elif 硬编码多模式 | RoutingPolicy 接口 + 策略类集合 |
| 与 HTTP 解耦 | pick_rank 耦合 header+body | RequestContext 抽象 |
| 目标抽象 | 只返回 int rank | Backend（可扩展健康/多上游）|
| 测试 | 策略测试还行 | 三层层层可单测，Router 纯逻辑最易测 |
| 扩展新 DP 拓扑 | 改 pick_rank | 加新 Policy 类 |

---

## 六、目录结构（对齐三层）

```
src/prefix_hash_router/
├── ingress/            # 接入层（HTTP 适配）
│   ├── http.py         # 收请求/读 body/构造 RequestContext（可多框架适配）
│   └── main.py         # server 入口/参数
├── router/             # 路由核心（纯逻辑，与 HTTP 无关）
│   ├── context.py      # RequestContext
│   ├── backend.py      # Backend 定义
│   ├── policy.py       # RoutingPolicy 接口 + ChainedPolicy
│   └── policies/       # 具体策略类（可插拔）
│       ├── prefix_hash.py     # PrefixHashPolicy
│       ├── session_affinity.py# SessionAffinityPolicy
│       ├── consistent.py      # ConsistentHashPolicy
│       ├── round_robin.py     # RoundRobinPolicy
│       └── overload.py        # OverloadGuard
└── dispatcher/         # 转发层
    ├── forward.py      # 透传逻辑（注入头/SSE/hop-by-hop）
    └── client.py       # 把 Backend 变成真实 HTTP 转发
```

---

## 七、结论

推荐采用**三层（Ingress / Router / Dispatcher）+ 两个抽象（RequestContext / Backend / RoutingPolicy）**
的架构。它比"策略/反代两块"更清晰：
- **Router 是核心**，策略是**可插拔 Policy 集合**，转发是 **Dispatcher**。
- 职责/可测性/可扩展性都更好，且与业界（SGLang SMG 的 policy 模式）一致。
