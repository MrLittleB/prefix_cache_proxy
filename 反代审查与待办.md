# 反代（Reverse Proxy）全面审查与待办清单

> 对 prefix-hash-router 的 Ingress/Dispatcher 反代层做系统性审查，
> 记录已处理的问题与尚未考虑的边界情况，供后续迭代。

---

## 一、已处理（含提交）

| # | 问题 | 处理 | 提交 |
|---|---|---|---|
| 1 | 用户端断开 → vLLM 仍空跑 | 流式用 read1 + 非阻塞 select 检测断开，主动 close 上游 | c931237 |
| 2 | metrics 被环境 http_proxy(7897) 劫持 | 构建 ProxyHandler({}) 直连 opener | a7ba00c |
| 3 | 请求体无大小限制，可能打满内存 | 新增 --max-body-size，超限 413 | a7ba00c |
| 4 | 慢客户端/半开连接无限阻塞占线程 | 给 _Handler 设置读写超时 | d8d5799 |
| 5 | 过载保护无真实数据源 | 接 MetricsCollector(running/waiting 加权) | 33f56c3 |
| 6 | 共享大前缀塌缩热点 | SessionAffinity(x-smg-routing-key) + HRW + OverloadGuard | 75da9dd |
| 7 | 上游连接失败/超时无明确响应 | forward_response 返回 502 + JSON 错误体 | 21980b7 |
| 8 | header 大小写敏感导致 SessionAffinity 失效 | Ingress 统一转小写 header key（X-Session-Id 也能命中） | fd1b39b |
| 9 | chunked 请求体带 chunk 扩展(size;ext)解析失败 | _read_body 支持 size[;ext]（RFC7230 §4.1.1） | fd1b39b |
| 10 | 畸形 body(JSON 字符串/数组)导致崩溃 | RequestContext/extract_* 防御 isinstance(dict/list) | fd1b39b |
| 11 | 多模态 content(list) 哈希键不稳定 | _msg_text 仅取文本部分作 key | fd1b39b |
| 12 | Router/Dispatcher 内部异常导致客户端悬挂 | _handle 捕获 → 显式 500 JSON | fd1b39b |
| 13 | 线程爆炸（高并发无上限） | _ThreadingServer 加 BoundedSemaphore 限流(--max-workers) | fd1b39b |
| 14 | 上游 path base 前缀映射 | build_target_path 支持 upstream_base（/v10 不误判命中 /v1） | fd1b39b |
| 15 | radix 树会无限增长 | LRU(children判定共享) + max_nodes 有界 + 默认 100000 | 5274672/fdacf15/24241d1 |
| 16 | 会话键覆盖不足(openclaw/pi/claude/codex/dsh) | 扩展 conversation/thread/pi/user 等会话键 | e551693 |
| 17 | 多消息接口(anthropic/responses)不支持 | _is_chat_request 三类接口 + messages 三格式提取 | e94854e |
| 18 | 接口分类基于真实 vLLM 路由 | 精确白名单，仅消息生成接口加 rank，其余透传 | 4d3fe09 |
| 19 | HEAD 返回 501(健康检查误判) | 补 do_HEAD 透传(不加 rank) | aa091fb |
| 20 | radix refcount 虚高(多轮计数) | 移除 refcount，用 children 结构判共享 | b9d79c2 |
| 21 | 三协议真实格式核对(developer/input_text/null系统) | 网上核对官方 SDK，修正 developer role 与不重复 system | 277ecac/116a2d1 |
| 22 | 无 .env 配置、源码硬编码真实地址/密钥 | 新增 env_loader.py(.env)，命令行优先、.env 兜底；清除源码硬编码 | 本次 |
| 23 | 过载"1 个请求 vs 其它全 0"被相对平均误判 spill → rank 乱跳 | 过载改两层：min-load(绝对门槛,默认5) + > avg×skew(相对失衡) | 本次 |
| 24 | waiting 固定高倍权重放大瞬时毛刺 | waiting 改渐进权重(waiting-weight=上限4)：第1个2/第2个3/第3个起4 | 本次 |
| 25 | rank 漂移/异常命中难以定位 | 新增 [radix-debug](命中深度/断点) + [overload] SPILL + [forward-debug](完整地址/body结构) | 本次 |
| 26 | DSH 经代理 400/401 定位 | 已实测：代理+直连标准请求均 200，确证问题在 DSH 侧(tools 缺 name/responses、鉴权/messages) | 本次验证 |

---

## 二、尚未处理 / 需进一步验证

### 2.1 连接管理（客户端 keep-alive）
- 实测代理回给客户端 **无 Connection 头**，`_Handler` 用 HTTP/1.1。
- 客户端连接是否保持 keep-alive、会不会悬挂/半开，**未充分验证**。
- 实际验证：上游返回 **Content-Length** 时 keep-alive 正常复用；上游返回 **chunked**（SSE）时
  `BaseHTTPRequestHandler` 自动置 `Connection: close`（客户端靠连接关闭判断 body 结束，正确但不复用）。
- 建议（后续）：显式管理 `Connection` 头，对非流式响应也尽量保持 keep-alive。

### 2.2 连接复用（到上游）
- `forward_response` 每请求新建一个到 vLLM 的 TCP+TLS 连接，`finally` close。
- **无连接池**：高并发下每次请求都有建连开销（TLS 握手~几十ms）。
- 非 bug，但高并发性能优化点。可用连接池（urllib3/httpx）或 keep-alive 复用。

### 2.3 上游错误传播（已完成基本验证）
- ✅ 上游非 2xx（400/404/429/500/503）：透传 status/headers/body —— test_error.py 已验证。
- ✅ 上游连接失败：返回 502 + JSON 错误体 —— test_error.py 已验证。
- ✅ Router/Dispatcher 内部异常：返回 500 + JSON 错误体 —— test_error.py 已验证。

### 2.4 非 POST 方法透传 / 非 chat 请求 / 多消息接口
- ✅ GET/PUT/PATCH/DELETE/OPTIONS 都映射到 `_handle`，会透传。
- ✅ **按真实 vLLM 启动日志路由清单精确白名单**：仅 3 个真实消息生成接口注入
  `X-data-parallel-rank` 头（做前缀路由）：`/v1/chat/completions`、`/v1/messages`、`/v1/responses`
- ✅ **透传不加 rank**：`/models`、`/health`、`/metrics`、`/ping`、`/load`、`/version`、`/docs`、
  `/completions`、`/tokenize`、`/detokenize`、`/invocations`、`/generative_scoring`、
  `/scale_elastic_ep`、`/is_scaling_elastic_ep`、`/inference/v1/generate`、
  `render/derender/count_tokens/batch`、responses 查询/取消、GET/OPTIONS/HEAD 等。
- ✅ `RequestContext.messages` 支持三种格式标准化提取（且向前兼容标准 messages）。
- ✅ `do_HEAD` 已补：HEAD 也透传(不加 rank)，健康检查/探活不再返回 501。

### 2.5 线程爆炸（高并发）
- ✅ 已加 `BoundedSemaphore` 限流：默认 256 并发，`--max-workers` 可调。
- 超出的连接在 accept 队列排队，不会无限起线程。

### 2.6 慢客户端读 body 超时的 chunked 分支
- `_read_body` chunked 分支 `rfile.readline()/read()` 依赖 `_Handler.timeout`（socket 级超时），
  已覆盖——慢速/半开 chunked 客户端会在 timeout 后断开。已验证基础场景。

### 2.6 慢客户端读 body 超时的 chunked 分支
- `_read_body` 的 chunked 分支 `rfile.readline()`/`rfile.read()` **未设显式读超时**（依赖 _Handler.timeout，已加但需验证 chunked 场景）。

### 2.7 SSE 心跳 / 流式超时
- 若 vLLM 长时间不吐数据（首 token 慢），客户端可能超时。
- 当前无空闲超时/心跳策略，需确认 DSH 侧期望。

### 2.8 TLS 上游的证书
- `forward.py` 用 `ssl._create_unverified_context()` 跳过校验（因自签名）。
- **安全权衡**：应支持配置 CA/校验，或明确文档说明。

### 2.9 多实例一致性（radix 相关）
- ✅ **`mode=radix` 已实现为单实例方案**：`PrefixRadixTree` 做 message 级最长连续前缀精确匹配，
  每次决策后 learn 回填实际归属 rank；冷启动回退 round-robin。追加/中间插入/截断/改开头都能
  精确处理（天然只锚定"从头连续稳定"前缀）。
- ✅ **默认 radix 树有上限**：`build_router` 默认 `radix_max_nodes=100000`（可 `--radix-max-nodes` 调、
  设 None 关闭），LRU 淘汰最冷会话、树保持 <= 上限，不无限增长。
- ⚠️ **仍是有状态 + 仅单实例可用**：树是进程内状态；多实例需共享/同步这棵树。
- 多实例都做前缀匹配时，同一前缀可能被不同实例路由到不同 DP（vLLM #52463 SO_REUSEPORT 类似）。
- **演进项**：多实例需共享亲和表/coordinator（对齐 vLLM #52463 完整修复方向）。

### 2.10 日志与可观测
- 目前只有简单 access log，无结构化日志（request_id、rank、命中率、耗时）。
- ✅ `RadixPrefixPolicy` 暴露 `router.radix_tree.size()` 可观测树规模。
- ✅ **radix 命中率/分布统计**：`router.radix_stats`（total/hits/misses/每 rank 分布），
  可判断 radix 是否有效、有无热点。
- ✅ **MetricsCollector.diagnosis()**：统计实际 vLLM engine 数 vs 配置 dp_size，
  启动时 main.py 自动检测 mismatch 并告警（防 dp 配置错误漏服务）。

---

## 三、结论 / 优先级建议

**近期应做（影响正确性/稳定性）**
- 2.3 上游错误传播验证 + 502 处理
- 2.1 客户端 Connection 策略明确
- 2.5 线程池上限（防线程爆炸）

**中期优化**
- 2.2 上游连接池
- 2.10 结构化日志/指标
- ✅ radix 树的 **LRU 淘汰**（已实现：max_nodes 有界 + 淘汰最冷叶子，保留共享前缀）
- ✅ radix LRU 用 **children 结构判定共享前缀**（替代 refcount），避免同一会话多轮
  追加导致共享前缀 refcount 虚高(计轮次而非会话数)的计数问题
- ✅ **radix 只在叶子标记 rank**：不同会话(叶子不同)分散到不同 DP 用各自独立 KV cache，
  解决"每 DP cache 有限、共享前缀会话全挤 rank0 互相顶掉"的问题；同一会话多轮仍粘一起。
- ✅ **冷启动 RadixFallbackPolicy**：有会话键 HRW 稳定分散 / 无会话键随机打散 + 过载感知，
  避免顺序轮转局部热点、避免共享 system 塌缩、避免冷启动落到过载 DP。
- ✅ 静态 load 长度校验 + load_provider 运行时长度诊断（防过载 rank 错配）
- ✅ 修复 radix 冷启动随机落过载 rank 导致的同前缀不一致(偶发测试失败)
- ✅ **过载判定改成两层**：`--min-load-for-overload`(绝对门槛,默认5) + `> avg×load_skew`(相对失衡)，
  避免"1个请求 vs 其它全0"这类低负载被相对平均误判而 spill
- ✅ **waiting 用渐进递增权重**：`--waiting-weight`(上限,默认4)=第1个2、第2个3、第3个起4，
  避免瞬时小 waiting 被固定高倍放大；负载= running + progressive_waiting_score(waiting)
- ✅ **诊断增强**：`[radix-debug]`(命中深度/断点/断点内容) + `[overload] SPILL`(触发原因/阈值/目标)，
  可精确定位 rank 漂移源于"内容不稳定断档"还是"过载 spill"
- ✅ **`.env` 配置**：UPSTREAM/METRICS_TOKEN/HOST/PORT/DP_SIZE/MODE/WATING_WEIGHT/MIN_LOAD_FOR_OVERLOAD 等可入 `.env`，
  命令行优先、.env 兜底，源码不再硬编码真实地址/密钥
- radix 树的**持久化**（重启保留归属，可选演进）

**演进/多实例**
- 2.9 共享亲和表/radix 树（对齐 vLLM #52463 完整修复方向）
- 2.8 TLS 证书策略

---

*审查日期：2026-08；基于当前实现 + 实测逐步补充。*
