# vLLM 多 DP 前缀缓存路由 · 完整方案

> 一份整合文档：从问题确认 → 业界调研 → 可行性核实 → 方案设计 → 自我审核修正 → 最终难点 → 验证/里程碑。
> 面向"为 vLLM 多 DP 集群实现前缀感知哈希路由"的完整交付。

---

## 目录
1. [问题与已确证的事实](#1-问题与已确证的事实)
2. [业界方案调研](#2-业界方案调研)
3. [可行性核实](#3-可行性核实)
4. [方案设计](#4-方案设计)
5. [自我审核与修正](#5-自我审核与修正)
6. [最终难点确认](#6-最终难点确认)
7. [验证计划与里程碑](#7-验证计划与里程碑)

---

# 1. 问题与已确证的事实

## 1.1 问题
vLLM 由 8 个数据并行（DP）副本组成，各 DP 独立、**互不共享 KV 前缀缓存**。
默认 DP 路由是**前缀无关的纯负载均衡**（`core_client.get_core_engine_for_request`
按 `waiting*4+running` 选最闲 DP），把相同前缀/多轮会话散到不同 DP → 前缀缓存命中率低。

## 1.2 三角验证（全部完成）
| 维度 | 结论 |
|---|---|
| **源码** | 远程只读核实调用链：`serving.py` 读 `X-data-parallel-rank` 头 → `EngineCoreRequest.data_parallel_rank` → `core_client.get_core_engine_for_request`（默认 `waiting*4+running` 纯负载均衡，无前缀匹配）|
| **实测** | 全新前缀 5500 tokens：默认负载均衡 **1/8=12.5%**，固定 rank **7/8=87.5%**；跨 rank 切换立即冷启动（各 DP 缓存独立）|
| **社区** | GitHub issue #52463、PR #43483/#52464 与本问题完全一致；根因 #47420 已合并，完整修复未实装 |

## 1.3 关键澄清：不是"以前可用后来坏了"
- v0.23.0 之前能到 81.6%，是负载均衡 tie-break **固定扫描起点**偶然把每个 client 钉到同 rank
  **碰巧**获得的缓存局部性，**并非设计保证**。
- 根因 **#47420**（为修公平性把 tie-break 改成轮转）**已于 2026-07-06 合并**，把这个巧合打破 →
  命中率跌到 7.8%~52.9%。这是当前 main 正在发生的现状。
- 完整修复（前缀感知路由）**至今未实装**：需要动 DP coordinator / 共享亲和表层，属设计决策，卡在 review。

---

# 2. 业界方案调研

## 2.1 vLLM 官方/社区（直接相关）
| 条目 | 内容 | 状态 |
|---|---|---|
| **#47420** | Rotate tie-break（根因，轮转破坏前缀局部性）| **已合并 2026-07-06** |
| **#52463** | Bug 报告：多轮前缀命中暴跌（8×H100/DP4+EP 实测 81.6%→7.8~52.9%）| Issue |
| **#43483** | `--data-parallel-prefix-cache-lb` 前缀感知路由（前端算 prompt block-hash + 本地归属表）| **Open 未合并** |
| **#52464** | prefix-affinity tie-break（平局时记前缀首块归属、偏好该 rank）| **Open 未合并** |

- vLLM 官方倾向"前端算 block-hash + 本地归属表"（#43483）或"平局前缀亲和"（#52464）；
  作者明言**多前端进程下的共享亲和表/coordinator 才是完整答案**。

## 2.2 SGLang（前缀缓存路由最成熟）
| 条目 | 方案 | 要点 |
|---|---|---|
| **#26612** | `prefix_match` | `blake2b(前4k token 的256-token窗口) % dp_size` |
| **#31170** | `prefix_affinity` | routing-key 亲和 + 过载保护（解决共享大前缀塌缩）|
| **#15935** | `PrefixHash` | 前 N token 哈希 + 一致性哈希环 + load_factor 过载保护（已合并）|
| **SMG Gateway** | `consistent_hashing` / `prefix_hash` / `bounded_consistent_hashing` | 网关层会话亲和，含过载保护 |
| **#26236** | terminal-prefix affinity | 只 sticky 完整会话命中，共享前缀首次放 round-robin |
| **#26285** | cache-aware request_text | 用完整 chat history 构造文本 |
| **#2114** | cache-aware router | 无 tokenizer 网关层字符串前缀匹配，可达 ~2x 吞吐（已合并）|
| **#34513** | RFC | 无 routing key 的 agent session affinity |

## 2.3 LiteLLM / OpenRouter（网关 session 亲和）
- **LiteLLM #34766**：从请求内容推导 session key 兜底。
- **OpenRouter**：sticky key 三层兜底链（显式 session_id → `prompt_cache_key` → …）。

## 2.4 LMCache / KV 共享路径（不同思路）
- 用 mp-coordinator + 共享 L1/L2 让多副本共享 KV（不靠路由），与路由方案**互补**，但部署重、依赖 KV connector，短期不采用。

## 2.5 通用技术基础
- 一致性哈希环（Envoy ring hash、nginx hash、SMG consistent_hashing）：有状态/亲和路由的标准手段。
- 关键权衡：**严格亲和 vs 负载均衡**——亲和过度会热点，需过载保护。

---

# 3. 可行性核实
## 3.1 vLLM `X-data-parallel-rank` 头有效（已实测）
固定 rank=N 后命中 87.5%，跨 rank 切换冷启动；源码确认有头直接 `self.core_engines[rank]`。

## 3.2 目标环境无 tokenizer（关键约束）
- SGLang #15935 `PrefixHash`：**gRPC router 有 token 才做 token 哈希；HTTP router 无 token 优雅回退**。
  → **代理层做不了 token 级哈希**。
- SGLang #26612 `prefix_match`：在 **engine 内（有 tokenizer）** 做 token 哈希 → 代理层不具备该条件。

## 3.3 字符串级前缀匹配可行（重要反证）
- SGLang #2114 cache-aware router（已合并）：**无 tokenizer 网关层用字符串 `request_text` + radix tree
  前缀匹配可达 ~2x 吞吐** → 字符串级方案可行。
- SGLang #26285：request_text 须用**完整 chat history**，避免共享 system 下不同会话看起来一样。

## 3.4 热点是公认问题（须过载保护）
SGLang #15935/#33625/#31170 一致：严格亲和会热点，需 `load_factor` 过载保护。

## 3.5 KV 共享路径本集群不可行（演进项）
需 KV connector / 共享 coordinator，部署级改动，本任务不做，仅记录。

---

# 4. 方案设计

## 4.0 三层架构（已实现）

采用 **Ingress / Router / Dispatcher 三层** + **RequestContext / Backend / RoutingPolicy 抽象**。

```
                       ┌─────────────────────────────────────┐
   HTTP 请求 ────────▶ │  Ingress（接入层）                    │
                       │  收请求、读 body、构造 RequestContext │
                       └──────────────────┬──────────────────┘
                                          │  RequestContext（与 HTTP 无关）
                                          ▼
                       ┌─────────────────────────────────────┐
                       │  Router（路由核心，纯逻辑）            │
                       │  ChainedPolicy 组合多个 RoutingPolicy │
                       │  policy(ctx) -> Backend              │
                       └──────────────────┬──────────────────┘
                                          │  Backend（目标抽象）
                                          ▼
                       ┌─────────────────────────────────────┐
                       │  Dispatcher（转发层）                 │
                       │  注入 rank 头、透传 header/body/SSE   │
                       └──────────────────┬──────────────────┘
                                          ▼
                                   vLLM 多 DP 集群
                 resp.usage.prompt_tokens_details.cached_tokens（观测命中）
```

**核心抽象**：
- **`RequestContext`**：`{headers, parsed_body, raw_body, session_key}` —— Router 与 HTTP 解耦。
- **`Backend`**：`{dp_rank, inject_rank}` —— 路由目标抽象，从"int rank"升级为"目标后端"。
- **`RoutingPolicy` 接口**：`route(ctx) -> Backend | None` —— **策略 = 可插拔 Policy 类集合**，
  而非 if/elif 硬编码。加新策略 = 加一个类。

## 4.1 目录结构（三层已落地）

```
src/prefix_hash_router/
├── ingress/            # 接入层
│   └── server.py       # 收请求/读 body/构造 RequestContext → Router → Dispatcher
├── router/             # 路由核心（纯逻辑，与 HTTP 无关）
│   ├── context.py      # RequestContext
│   ├── backend.py      # Backend
│   ├── policy.py       # RoutingPolicy 接口 + ChainedPolicy
│   └── policies/       # 可插拔策略类
│       ├── prefix_hash.py      # PrefixHashPolicy
│       ├── session_affinity.py # SessionAffinityPolicy
│       ├── consistent.py       # ConsistentHashPolicy
│       ├── round_robin.py      # RoundRobinPolicy
│       ├── overload.py         # OverloadGuard
│       ├── _keys.py            # 共享：前缀/会话键提取
│       └── _hash.py            # 共享：blake2b / 一致性哈希
├── dispatcher/         # 转发层
│   └── forward.py      # 透传逻辑（注入头/SSE/hop-by-hop/Host/超时）
└── app.py              # build_router：组装 ChainedPolicy
main.py                 # CLI 入口
tests/                  # test_router / test_dispatcher / test_ingress_e2e
```

## 4.2 Router 策略设计
- 组合：`ChainedPolicy([SessionAffinityPolicy, CorePolicy])`（外加可包装 OverloadGuard）。
- 优先级：显式会话 key → 稳定共享前缀哈希 → round-robin（兜底）。
- 哈希：`blake2b(key) % DP`（非随机、跨进程稳定、不依赖 PYTHONHASHSEED）。
- 过载保护：首选 rank 负载 > 平均×load_factor 时退到同窗口最闲 rank。

## 4.3 哈希策略（并入策略块）
- 基本：`rank = blake2b(prefix_key) % DP`（O(1)、跨进程稳定、非随机）。
- 演进：一致性哈希环（DP 扩缩容时减少整体重映射）。
- 过载保护：首选 rank 负载 > 平均×load_factor 时退到同窗口最闲 rank。

## 4.4 前缀键构造（核心，归属策略块）
**修正后的两层策略**（避免"变化的 tail"矛盾）：
1. 有**显式会话/routing key**（`x-session-id`、`prompt_cache_key`、`session_id`）→ 用它（最强粘性）。
2. 否则默认 **稳定共享前缀哈希**：`blake2b(system + 稳定 preamble) % DP`。
   - 命中共享前缀缓存（同 system 落同 rank）
   - 用过载保护 + (可选)会话二级分流缓解"共享大前缀热点塌缩"

## 4.5 Dispatcher（转发层）设计
- 职责：接收 Router 的 `Backend` → 注入 `X-data-parallel-rank: <rank>` → 原样转发。
- 透传：保留 Authorization 等所有 header，完整 body，SSE 流式逐块回传。
- 细节：hop-by-hop 头过滤、chunked 处理、显式设置上游 Host、超时。
- **纯执行**：不关心 Backend 为何选该 rank，只负责正确注入与转发（`dispatch.forward_response`）。

---

# 5. 自我审核与修正

## A. 自我审核发现（初稿薄弱点）
| # | 发现 | 修正 |
|---|---|---|
| A1 | **"system+tail 哈希"与"多轮缓存复用"矛盾**：tail 每轮变 → 各轮落不同 DP | 改为**对稳定共享前缀哈希** |
| A2 | "字符级前缀能命中 token 级缓存"无实测支撑 | 列为**最大不确定点，必须先跑 M0 实验** |
| A3 | PYTHONHASHSEED 与 blake2b/xxhash 重复矛盾 | 统一用非随机哈希，不依赖 PYTHONHASHSEED |
| A4 | 过载保护与命中率冲突未量化 | 明确重负载下牺牲命中换均衡 |
| A5 | 目标负载形态未明确 | 先定性"共享固定 system" vs "独立长多轮" |

## B. 二次联网核实（关键假设成立性）
- B1 代理层做不了 token 级哈希（SGLang #15935 证实）。
- B2 字符串级前缀匹配可行（SGLang #2114 已合并，~2x 吞吐）。
- B3 有状态（radix tree）vs 无状态（哈希）可互补；本方案走无状态。
- B4 过载/热点是公认问题。

---

# 6. 最终难点确认

| 难点 | 说明 | 应对 |
|---|---|---|
| **D1（最本质）** | 字符级前缀 vs token 前缀**对齐**问题，是否真命中 vLLM 缓存 | **先做 M0 最小实验**，不能预设达 87.5% |
| D2 | 多轮 key 稳定性（须基于稳定共享前缀）| 共享前缀哈希 + 显式 session key 增强 |
| D3 | 共享大前缀热点塌缩 | 过载保护 + 会话二级分流（命中率 vs 均衡取舍）|
| D4 | 无 tokenizer 的信息上限 | 以 A/B 实际增量验收 |
| D5 | 多前端进程/SO_REUSEPORT 打散亲和 | 单进程代理规避；严格需共享表（演进）|
| D6 | DP 扩缩容 `hash%N` 整体重映射 | 预留一致性哈希环 |
| D7 | 亲和 vs 公平冲突（vLLM #47420 教训）| 过载保护平衡 |

---

# 7. 验证计划与里程碑

## 7.1 A/B 对照
1. **baseline**：直连默认负载均衡（~12.5%）
2. **固定 rank**：`X-data-parallel-rank: 0`（~87.5%）
3. **本方案**：前缀哈希路由（目标：同前缀 ≥85%，多前缀仍平衡）

指标：`cached_tokens` 命中率、平均缓存 token 比例、多前缀分布均匀度、TTFT/吞吐。

## 7.2 里程碑
- [ ] **M0（前置）** D1 最小实验：字符相同→固定 rank 命中；共享前缀+变化后缀→多轮命中
- [x] M1 骨架：三层架构（Ingress/Router/Dispatcher）+ 透传 + 注入 rank，单测/e2e 通过
- [x] M2 前缀键提取（稳定共享前缀，版本 1）
- [x] M3 哈希 + 路由（可插拔 Policy）+ 过载保护
- [ ] M4 A/B 实测（baseline / fixed / hash）对接真实 vLLM
- [ ] M5 上线文档

---

## 附：来源索引
- vLLM：PR #47420、#43483、#52464；Issue #52463
- SGLang：PR #26612、#31170、#15935、#26236、#26285、#2114、#34513(RFC)、#33625
- LiteLLM #34766；OpenRouter sticky key；LMCache #3248/#3488

*整合日期：2026-08；全部基于各仓库公开 issue/PR 实时抓取 + 本集群源码/实测验证。*

---

# 8. 实测发现与关键修正（对接真实 vLLM）

## 8.1 发现：纯前缀哈希在"共享大前缀"下会塌缩成热点
对真实 vLLM 经代理测试时发现：32 个不同前缀若**共享同一个巨大 system 前缀**，且只用
`PrefixHashPolicy`（按 system 哈希），则**全部塌到同一 rank**（实测 {7:32}）。
这与 SGLang #26612/#31170 描述的"共享前缀塌缩"完全一致。

## 8.2 修正（参考 SGLang #31170 prefix_affinity）
1. **SessionAffinityPolicy 支持 `x-smg-routing-key`**（业界标准头）：不同 session 给不同 key →
   各自落不同 rank，**从根本上避免共享大前缀塌缩**；进程内实测 256 个不同 key 共享同一前缀
   → 分散到全部 8 个 rank（原先塌到 1 个）。
2. **改用 HRW(Rendezvous) 哈希**（`hrw_hash_rank`）：rank 用稳定 id 评分，拓扑变化只重映射受影响者。
3. **OverloadGuard 过载 spill**：首选 rank 过载时按 HRW 候选排序 spill 到下一个可接受 rank，
   避免单 rank 被打爆。

## 8.3 实测结果（真实 vLLM）
- **同一前缀经代理连发**：5/5 命中（首轮已缓存后，cached=6400 全中）——前缀哈希保命中。
- **不同 session 共享同一 system 前缀**：全部 HIT——共享前缀缓存在会话间复用。
- **进程内**：256 个不同 routing key → 分布 8 ranks；过载 spill 正常；未过载保持亲和。

## 8.4 结论
- **前缀哈希只保证"相同前缀→同 rank 命中"**，不能区分共享大前缀的不同会话 → 必须有
  **session/routing key（SessionAffinityPolicy）优先** + **OverloadGuard 防热点**。
- 这正是 SGLang #31170 的核心设计，我们已按此修正，三套测试 + 真实 vLLM 实测均通过。

## 8.5 真实 running/waiting 负载采集（过载保护的数据源）

**问题**：过载保护（OverloadGuard）不能只靠静态 mock 的负载——必须用上游 vLLM 每 DP 的
**真实排队/运行**状态。

**实现**（`metrics_collector.MetricsCollector`）：
- 从 vLLM `/metrics` 拉取 **`vllm:num_requests_running{engine=N}`** 与
  **`vllm:num_requests_waiting{engine=N}`** 两个 gauge（本集群每 DP 独立暴露，已验证）。
- 负载评分与 vLLM 内部一致：`load[i] = waiting[i]*W + running[i]`，默认 `W=4`
  （vLLM `core_client.py:1430` `score = waiting*4 + running`）。
- 后台线程轮询刷新；拉取失败保留上次值不影响路由。
- `main.py` 默认从 `--upstream` 推导 `/metrics` 并接入 `build_router(load_provider=...)`；
  每请求实时取当前负载做过载判断（`--no-overload` 可关）。

**验证**：
- 单元测试：解析真实格式、加权负载、HTTP 拉取、负载进 router 触发 spill。
- 真实 vLLM：成功从 8 个 engine 拉到 running/waiting（空闲全 0），代理链路无报错。
